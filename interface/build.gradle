bootJar {
    archiveBaseName = "interface"
    archiveVersion =  '1.0'
}

dependencies {
    implementation project(":service")

    implementation 'org.springframework.boot:spring-boot-starter-thymeleaf'
    implementation "org.springdoc:springdoc-openapi-security:${docVersion}"
}
